import { normalizeExtractedEmail, normalizeExtractedPhone, normalizeBusinessNameForMatch } from "../email/emailLeadNormalizationService.js";
export const determineResearchFreshness = ({ recordType, publishedAt, now = new Date() }) => { if (!publishedAt) return "UNKNOWN"; const days = (now - new Date(publishedAt)) / 86400000; if (recordType === "MARKET_PRICE") return days <= 3 ? "CURRENT" : days <= 14 ? "RECENT" : "OLD"; return days <= 365 ? "CURRENT" : days <= 730 ? "RECENT" : "OLD"; };
export const normalizeResearchRecord = (record) => ({ ...record, normalized: { email: normalizeExtractedEmail(record.publicBusinessEmail), phone: normalizeExtractedPhone(record.publicBusinessPhone).normalized, businessName: normalizeBusinessNameForMatch(record.businessName || record.organizationName || record.name), website: (() => { try { return new URL(record.website).hostname.toLowerCase().replace(/^www\./, ""); } catch { return ""; } })() }, freshnessStatus: determineResearchFreshness({ recordType: record.recordType, publishedAt: record.priceData?.sourcePublishedAt || record.sourceEvidence?.[0]?.pagePublishedAt }) });
