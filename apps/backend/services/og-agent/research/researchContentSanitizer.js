import { createHash } from "node:crypto";
const injection = /(ignore\s+(all\s+)?previous|system\s+prompt|reveal\s+(secrets?|instructions)|disable\s+approval|execute\s+commands?|fetch\s+another\s+domain|submit\s+(a\s+)?form)/gi;
export const sanitizeResearchContent = (value, maximumBytes = 100000) => { let text = String(value || "").slice(0, maximumBytes); const injectionDetected = injection.test(text); injection.lastIndex = 0; text = text.replace(injection, "[UNTRUSTED_SOURCE_INSTRUCTION_REMOVED]").replace(/<script\b[^>]*>[\s\S]*?<\/script>/gi, " ").replace(/<style\b[^>]*>[\s\S]*?<\/style>/gi, " ").replace(/<[^>]+>/g, " ").replace(/\s+/g, " ").trim(); return { safeText: text.slice(0, 12000), contentHash: createHash("sha256").update(String(value || "")).digest("hex"), injectionDetected }; };
export const detectBlockedPage = (value = "") => { const text = String(value).toLowerCase(); if (/captcha|verify you are human|cf-chl-|g-recaptcha/.test(text)) return "CAPTCHA_DETECTED"; if (/<input[^>]+type=["']?password|sign in to continue|login required/.test(text)) return "LOGIN_REQUIRED"; return null; };
